## Packages
framer-motion | For smooth page transitions and entry animations
lucide-react | Beautiful icons for the interface
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Integration with Google Gemini Chatbot API is required.
SSE streaming for chat messages needs to be handled in the chat component.
Resources API is read-only (GET).
